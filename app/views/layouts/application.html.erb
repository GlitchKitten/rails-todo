<!DOCTYPE html>
<html>
<head>
  <title>Amber Mulholland | ToDo</title>
  <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css" rel="stylesheet">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'tinysort.min' %>
  <%= csrf_meta_tags %>
  
</head>
  
  <nav>
    <div class = "navbar navbar-header navbar-inverse navbar-fixed-top" role = "navigation">
      <div class = "container-fluid">
        <div class = "project">
          <li><%= link_to "ALL", todos_path %></li>
        </div>
        <div class = "navbar-header" id="heading">
          <button type = "button" class = "navbar-toggle" data-toggle = "collapse" data-target = ".navbar-collapse">
            <span class = "sr-only"> Toggle Navigation</span>
            <span class = "icon-bar"></span>
            <span class = "icon-bar"></span>
            <span class = "icon-bar"></span>
          </button>

          <div class = "navbar-collapse navbar-header collapse">
            <div class = "nav navbar-nav">
              <li><%= link_to "Urgent" %></li>
              <li><%= link_to "Pending" %></li>
              <li><%= link_to "Later" %></li>
              <li> 
                <%= form_tag(todos_path, method: "get", id: "search_form") do %>
                  <%= text_field_tag :search, params[:search] %>
                    <button type="button" class="btn btn-default" aria-label="Left Align">
                    <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
                  </button>
                <% end %>
              </li> 
              </br>
            </div>
          </div>
        </div>
      </div>
    </div>  
  </nav> 
  
  <body>
    <header>
      <h1>My Todos</h1>
    </header>
    
    <div class = "todo_form container">
      <%= form_for Todo.new, remote: true do |f| %>
        <div class="form-group priority">
          <%= f.radio_button :priority, 'Urgent'%> 
          <%= f.label :priority, 'Urgent', value: 'Urgent'%>
          <%= f.radio_button :priority, 'Pending' %>
          <%= f.label :priority, 'Pending', value: 'Pending' %>
          <%= f.radio_button :priority, 'Later', checked: 'checked' %>
          <%= f.label :priority, 'Later', value: 'Later'%>
        </div>
      
        <div class="form-group">
          <%= f.text_field :description, placeholder: "what needs doing?",  class: "form-control form_area" %>
        </div>

        <div class="button">
          <%= f.submit 'Submit' %>
        </div>
      <% end %>
    </div>
    
  <% if @todos.present? %>
    <ul>
    <%= render @todos %>
    </ul>  
  <% else %>
    <p id = "error_message">There are no products containing the term(s) <%= params[:search] %>.</p>
  <%end%>
  </body>

</html>
